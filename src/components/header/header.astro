---
import HeaderComp from './header'
import { getEntry } from 'astro:content'

const menu = await getEntry('menu', 'menu')
---

<header class="px-4 py-8">
  <HeaderComp menu={menu} client:idle />
</header>
